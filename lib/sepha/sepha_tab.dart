import 'package:flutter/material.dart';

class SephaTab extends StatelessWidget{
  const SephaTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.indigo,
    );
  }
}
